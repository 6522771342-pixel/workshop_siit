{
  "name": "workshop-siit-hub",
  "private": true,
  "dependencies": {
    "wrangler": "^3.101.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "scripts": {
    "build": "npm run build:candy && npm run build:life && cp index.html assets_dir/",
    "build:candy": "cd candy-crush && npm install && npm run build",
    "build:life": "mkdir -p assets_dir/smart-life && cp smart-life/index.html assets_dir/smart-life/",
    "postinstall": "npm run build"
  }
}