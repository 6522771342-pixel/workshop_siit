# GEMINI.md - AI Assistant Guidelines & Technical Reference

## 🛠 Technical Reference

### 1. Architecture: Vanilla vs Frameworks
- **Decision Matrix:** สำหรับแอปพลิเคชันขนาดเล็กถึงกลางที่มี Logic ไม่ซับซ้อนมาก การเลือกใช้ **Plain HTML/Vanilla JS** ให้ประสิทธิภาพที่สูงกว่า (Zero-bundle overhead) และความเสถียรในการ Deploy ที่มากกว่าเมื่อเทียบกับ Modern Frameworks
- **Performance:** โหลดได้รวดเร็วระดับ Sub-second เนื่องจากเบราว์เซอร์สามารถประมวลผลไฟล์ได้โดยตรงไม่ต้องผ่านขั้นตอน Hydration

### 2. Cloudflare Workers (Assets-only) Deployment
- **Configuration:** ใช้ `wrangler.toml` เป็นมาตรฐานหลัก โดยตั้งค่า `[assets]` ให้ชี้ไปยังโฟลเดอร์ที่มีไฟล์ `index.html`
- **Lockfile Integrity:** ระบบ Build ของ Cloudflare (เช่น `npm ci`) มีความเข้มงวดสูง ไฟล์ `package.json` และ `package-lock.json` ต้องซิงค์กัน 100% เสมอ
- **Binding Rule:** ในโปรเจกต์ที่เป็น Assets-only ห้ามใช้ `binding = "ASSETS"` เพราะจะทำให้การ Deploy ล้มเหลว

### 3. State & Theme Management
- **Persistence:** ใช้ `localStorage` ควบคู่กับ `JSON.stringify/parse` เพื่อรักษาข้อมูลผู้ใช้โดยไม่ต้องมี Database
- **Theme Engine:** ใช้ Class-based strategy (เช่น `.dark` class บน `<html>`) ร่วมกับ Tailwind CSS เพื่อความง่ายในการสลับธีมและประสิทธิภาพในการ Render
- **Global Scope:** ในไฟล์เดี่ยว (Single-file app) การแนบฟังก์ชันสำคัญไว้กับ `window` object (เช่น `window.toggleDarkMode`) ช่วยให้มั่นใจว่า Event Attributes ใน HTML จะหาฟังก์ชันเจอเสมอ

---

## 🧠 Lessons Learned

### 1. Deployment & Environment
- **CI/CD Friction:** โฟลเดอร์มาตรฐานอย่าง `dist` หรือ `build` อาจถูกระบบ CI/CD บางตัวมองข้ามหรือลบทิ้งอัตโนมัติ การตรวจสอบ Path ในสภาพแวดล้อมจริงเป็นสิ่งแรกที่ควรทำเมื่อ Build Fail
- **Wrangler Versioning:** การใช้ Wrangler เวอร์ชันล่าสุด (v4+) ช่วยลดข้อผิดพลาดเกี่ยวกับความเข้ากันได้ของระบบ Cloudflare รุ่นใหม่

### 2. User Experience (UX) Design
- **Custom vs Native Validation:** Native Form Validation (เช่น `required` attribute) อาจแสดง UI ที่ไม่เข้ากับดีไซน์แอป หรือทำให้ Logic ภายในสับสน การใช้ Custom Validation ผ่าน JS และแจ้งเตือนด้วย Toast System ให้ประสบการณ์ผู้ใช้ที่พรีเมียมกว่า
- **Proactive Feedback:** การใช้ Toast Notifications แทน `alert()` ช่วยให้ Flow การทำงานของผู้ใช้ไม่ถูกขัดจังหวะ

### 3. Workflow Optimization (Agent-Specific)
- **Non-Interactive First:** ทุกคำสั่งที่รันผ่าน AI Agent ต้องใช้ flag `-y` หรือ `--yes` เพื่อป้องกันอาการค้างในขั้นตอนการถามยืนยัน
- **Process Persistence:** การรันเซิร์ฟเวอร์จำลองในพื้นหลังควรใช้ `nohup` เพื่อให้ Process ยังคงทำงานต่อไปได้แม้เซสชันการเชื่อมต่อจะรีเฟรช

---
*Last Updated: 2025-12-26*
